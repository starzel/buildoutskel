# This contains a sample of sensitive information
# Copy this to secret.cfg and add the passwords
[buildout]
user = starzel

# only used if a new Data.fs will be created
login = adminstarzel
password = starzel

